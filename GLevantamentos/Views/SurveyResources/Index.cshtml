@model IEnumerable<GLevantamentos.Models.SurveyResources>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Surveys)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blocks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Floorings)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Equipaments)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Surveys.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Blocks.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Floorings.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Equipaments.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
